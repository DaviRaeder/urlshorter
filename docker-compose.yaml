services:
  tiny_postgres:
    image: postgres:14.1
    container_name: tiny_postgres
    restart: no
    environment:
      - POSTGRES_USER=postgres
      - POSTGRES_PASSWORD=postgres
      - POSTGRES_DB=tiny
    ports:
      - 5432:5432
    volumes:
      - ./postgres-data:/var/lib/postgresql/data